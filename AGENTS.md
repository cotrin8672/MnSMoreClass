# 🤖 MnSMoreClass Agent ガイド

※応答は日本語で

## 📦 必須リポジトリ

- **優先参照**: `documents/`
  - 設計・仕様・手順は全て `documents/*.md` に集約済み。
  - トピック別の概要は後述の「📚 推奨ドキュメント」を参照し、常に最新版を確認すること。
- **補助参照**: `C:\Users\gummy\IdeaProjects\Mine-And-Slash-Rework`
  - `documents/` で把握できない詳細を確認する際のみ利用する。
  - 直接コードを読む場合は最小限の範囲に留め、変更は本リポジトリ側の仕様に沿って行う。

## 🧭 作業フロー

- **情報収集**: まず `documents/` を検索 (`Ctrl+Shift+F` など) して目的の章を見つける。
- **仕様確認**: ドキュメントで要件が不明瞭な場合は関連セクションを一通り精読し、参照リンクを辿る。
- **補完調査**: それでも不足する場合のみメインプロジェクト側 (`Mine-And-Slash-Rework`) の該当ファイルを確認する。
- **更新反映**: ドキュメントに変更が必要になったら先に `documents/` を更新し、コード変更はその後に行う。

## 📚 推奨ドキュメント

- **`documents/01_SystemOverview.md`**: プロジェクト全体像と依存関係。
- **`documents/02_SpellSystem.md`**: Spell/SpellSchool の仕様、拡張手順、データ構造。
- **`documents/03_ExileEffects.md`**: Exile Effect・Perk 関連の設計方針。
- **`documents/04_KotlinAddon.md`**: Kotlin 実装ガイドライン、命名規則、DSL 利用方法。
- **各クラス計画 (`druid.md`, `ninja.md`, `alchemist.md` 等)**: クラス固有のスキル案とロードマップ。

ドキュメント内の参照リンクを最優先で辿り、情報の重複や矛盾があればドキュメントの整合性を保つようフィードバックを行う。

## 🛠️ 調査手段

- **ドキュメント内検索**: IDE 検索か外部ツールで `documents/` を対象にする。
- **コード参照が必要な場合**
  - `Mine-And-Slash-Rework` 内では `SpellSchool`, `Spell`, `Perk` などの該当パッケージを開く。
  - 参照したファイルは必ずドキュメントと突き合わせて認識を確認する。

## ✅ 対応ルールまとめ

- **レスポンス言語**: 常に日本語。
- **情報源の優先順位**: `documents/` → メインプロジェクト → その他 (外部資料が必要なら相談)。
- **作業前確認**: 既存ドキュメントを必ず読み、重複実装や仕様逸脱を避ける。
- **疑問点**: ドキュメントに追記が必要なら作業記録と併せて提案する。

---

**最終更新**: 2025-11-14
